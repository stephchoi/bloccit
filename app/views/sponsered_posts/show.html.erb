<h1><%= @sponsered_post.title %></h1>

<div class="row">
  <div class="col-md-8">
      <p><%= @sponsered_post.body %></p>
      <p>Price: $<%= @sponsered_post.price %></p>
  </div>
  <div class="col-md-4">
      <%= link_to "Edit", edit_topic_sponsered_post_path(@sponsered_post.topic, @sponsered_post), class: 'btn btn-success' %>
  </div>
</div>
  
